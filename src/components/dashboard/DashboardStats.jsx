import React from "react";

const DashboardStats = ({ stats }) => {
  return (
    <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
      {/* Stats content will be rendered by parent */}
    </div>
  );
};

export default DashboardStats;
